language: node_js
cache:
  yarn: true
  directories:
  - node_modules
notifications:
  email: false
node_js:
  - '8'

branches:
  except:
    - develop
    - master

script:
  - yarn prettify:check & yarn lint:check & yarn test:prod && yarn build

